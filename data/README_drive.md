# AHC Hackathon — Training and Public Test Data

- `train/` contains 12 class folders. Each class folder contains `videos/`, `videos.csv`, and `ground_truth.csv`.
- `test/` contains the 34-video public test set, `videos.csv`, and `ground_truth.csv`.
- Private evaluation ground truth is not included.
